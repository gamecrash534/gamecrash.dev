when:
  branch: main
  event: [push, manual]

steps:
  - name: deploy
    image: alpine
    commands:
      - mkdir -p /root
      - rm -rf /root/*
      - cp -r ./* /root/
    volumes:
      - /srv/static-sites/:/
